# Possible improvements

This is a page that details possible improvements to RAM_manager

## changing how scripts receive allocated RAM

instead of putting things in port 1, we can put them in the port with number == PID of the script

## Change some functions to a class

to be able to use self. to not need to pass 80 million arguments
> changing to an object instead
