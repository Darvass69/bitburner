import { afterAll, afterEach, beforeAll, expect, jest, test, beforeEach, describe } from '@jest/globals';

// npm test --coverage

/*
// setting up before and after tests -------------------------------------------
beforeAll(()=>{

})
// -----------------------------------------------------------------------------
beforeEach(()=>{

})
// -----------------------------------------------------------------------------
afterEach(()=>{

})
// -----------------------------------------------------------------------------
afterAll(()=>{

})
*/

/*
https://jestjs.io/docs/api
describe()
        .only
        .skip
                .each()

test()
    .concurrent 
                .only
                .skip
                        .each()
    .only
    .skip
            .failing
                        .each()
    .todo()
*/

test('test', () => {
        
})


